sprintf(문자열버퍼, "형식", 값들);
정수, 실수 같은 값을 문자열로 변환해서 버퍼에 저장해 줍니다.

printf가 화면에 출력하는 것처럼 보이지만, sprintf는 출력 결과를 문자열 변수에 저장합니다.

예:

c
char buf[20];
int num = 123;
sprintf(buf, "%d", num);   // buf에 "123" 이 들어감
